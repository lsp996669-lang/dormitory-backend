# 真机预览网络问题解决方案（最新）

## 问题根源

**Vercel 后端服务已无法访问**（连接被重置），已切换到 Coze 开发环境作为后端服务。

---

## 当前使用的后端域名

```
https://b287065f-8284-40e8-9ff3-f0a05eb1a25d.dev.coze.site
```

**验证状态**：✅ 已测试，API 返回 HTTP 200 OK

---

## 需要立即配置的域名

### 在微信小程序后台配置

1. **登录微信小程序后台**
   - 访问：[https://mp.weixin.qq.com](https://mp.weixin.qq.com)
   - 登录小程序账号

2. **进入开发设置**
   - 点击「开发」→「开发管理」→「开发设置」

3. **配置 request 合法域名**
   - 找到「服务器域名」区域
   - 在 **request 合法域名** 中添加：
     ```
     https://b287065f-8284-40e8-9ff3-f0a05eb1a25d.dev.coze.site
     ```

4. **等待生效**
   - 配置生效时间：5-15 分钟
   - 请耐心等待

5. **重新预览**
   - 在微信开发者工具中，点击「清除缓存」→「清除全部缓存」
   - 点击「预览」→ 扫码测试

---

## 快速验证步骤

### 1. 验证后端是否可访问

在浏览器或命令行中访问：
```
https://b287065f-8284-40e8-9ff3-f0a05eb1a25d.dev.coze.site/api/checkin/list
```

应返回类似：
```json
{
  "code": 200,
  "msg": "获取成功",
  "data": []
}
```

### 2. 清除缓存并重新预览

- 微信开发者工具：清除缓存 → 清除全部缓存
- 点击「预览」→ 扫码 → 在真机上测试

### 3. 测试功能

- 打开小程序，查看楼层信息是否加载
- 尝试登记入住
- 尝试查看入住列表

---

## 技术细节

### 小程序已重新构建
- 使用 `PROJECT_DOMAIN=https://b287065f-8284-40e8-9ff3-f0a05eb1a25d.dev.coze.site`
- 所有 API 请求会自动拼接正确的域名

### 网络请求示例
```typescript
// 前端代码
await Network.request({
  url: '/api/checkin/list'
})

// 实际请求
https://b287065f-8284-40e8-9ff3-f0a05eb1a25d.dev.coze.site/api/checkin/list
```

---

## 常见问题

### Q1: 配置后还是无法连接？

**检查清单：**
- [ ] 域名是否完全正确（不要漏掉任何字符）
- [ ] 域名是否以 `https://` 开头
- [ ] 是否已等待 15 分钟让配置生效
- [ ] 是否清除了开发者工具缓存
- [ ] 是否重新扫码预览

### Q2: 为什么之前配置的 Vercel 域名不行？

- Vercel 后端服务已无法访问（连接被重置）
- 已切换到 Coze 开发环境
- 新域名已验证可用

### Q3: 这个域名稳定吗？

- Coze 开发环境是临时的
- 建议后续部署到稳定的后端服务（如云服务器、Vercel 重新部署等）
- 当前仅用于真机预览测试

### Q4: 如何确认域名配置成功？

配置成功后，真机上的小程序可以：
- 正常加载楼层信息
- 正常登记入住
- 正常查看入住列表

---

## 长期解决方案建议

当前使用的是 Coze 开发环境域名，建议后续：

1. **重新部署到 Vercel**
   - 修复 Vercel 部署问题
   - 获得永久稳定的域名

2. **或部署到云服务器**
   - 购买云服务器（阿里云、腾讯云等）
   - 部署 NestJS 后端
   - 配置 HTTPS 证书

3. **或使用云托管服务**
   - 腾讯云 SCF
   - 阿里云函数计算
   - 华为云函数

---

## 紧急联系

如果配置后仍有问题，请提供：
1. 微信小程序后台的域名配置截图
2. 真机上的错误提示信息
3. 网络请求的详细日志（如果可以获取）

---

## 快速检查清单

- [ ] 登录微信小程序后台
- [ ] 进入「开发管理」→「开发设置」
- [ ] 添加域名：`https://b287065f-8284-40e8-9ff3-f0a05eb1a25d.dev.coze.site`
- [ ] 等待 15 分钟让配置生效
- [ ] 清除开发者工具缓存
- [ ] 重新预览并测试

---

**注意**：这个域名是 Coze 开发环境域名，仅用于临时测试。后续需要部署到稳定的后端服务。
